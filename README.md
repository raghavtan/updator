USAGE
```
$ ./reload.py -h 
usage: reload.py [-h] path url

Updation utilities

positional arguments:
  path        specify the home path for application directory to be updated
  url         specify the url to fetch tar

optional arguments:
  -h, --help  show this help message and exit

```